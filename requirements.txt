# Core networking and P2P dependencies
aiohttp>=3.9.0
asyncio>=3.4.3

# Data serialization
msgpack>=1.0.7

# Cryptography for content addressing and peer verification
cryptography>=41.0.0

# Audio processing (ISO/IEC 18040, ISO/IEC 18043)
numpy>=1.24.0
scipy>=1.11.0

# Web3 integration
web3>=6.0.0
eth-keys>=0.5.0
eth-account>=0.10.0

# TRON blockchain integration
tronpy==0.3.1

# PostgreSQL and database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0

# Environment configuration
python-dotenv==1.0.0

# Async Redis (for caching and queue)
aioredis==2.0.1

# FastAPI (already in use but ensure version)
fastapi>=0.104.0
uvicorn>=0.24.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
