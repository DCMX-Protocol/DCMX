# Core networking and P2P dependencies
aiohttp>=3.9.0
asyncio>=3.4.3

# Data serialization
msgpack>=1.0.7

# Cryptography for content addressing and peer verification
cryptography>=41.0.0
pyjwt>=2.8.0

# Audio processing (ISO/IEC 18040, ISO/IEC 18043)
numpy>=1.24.0
scipy>=1.11.0

# Web3 integration
web3>=6.0.0
eth-keys>=0.5.0
eth-account>=0.10.0

# TRON blockchain integration
tronpy>=0.4.0

# Database (SQLAlchemy ORM with async support)
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
aiosqlite>=0.19.0

# Environment configuration
python-dotenv>=1.0.0

# Async Redis (for caching and queue)
aioredis>=2.0.1

# Solidity compiler
py-solc-x>=2.0.0

# FastAPI (for REST API)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Payment integrations (Magic Eden + MIM)
stripe>=7.0.0  # Credit card payments
requests>=2.31.0  # HTTP client for payment APIs

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
